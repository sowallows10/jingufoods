<%#<h1>Schedules#index</h1>
<p>Find me in app/views/schedules/index.html.erb</p> %>
<%= stylesheet_link_tag 'index.schedule', :media => "all" %>
<div>
  <%= image_tag 'index.schedule_photo.jpg',class:'r'%>
    <header>
      <h1>俺の球場飯！　～神宮球場グルメ紹介～</h1>
      <div class="header-list">
      <ul>
      <%= link_to 'ログアウト', destroy_user_session_path, method: :delete %>
      <%= link_to "ホームへ", foods_index_path %>
      <%= link_to "球場飯投稿へ", new_tweet_path %>
      <%= link_to "球場飯一覧へ", tweets_path %>
      </ul>
    </header>


    <h2>球場飯を検索</h2>
      <div class="w">
        <%= form_tag({controller:"tweets",action:"index"}, method: :get) do %>
          <%= text_field_tag :search %>
          <%= submit_tag '検索する'  %>
          <p>※検索時、数字は半角で入力してください<p>
      </div>
    <% end %>

    <br>
    <br>
    <h2>対戦カレンダー</h2>

    <p id="notice"><%= notice %></p>

    <%#<table>
      <thead>
        <tr>
          <th>タイトル</th>
          <th>時間</th>
          <th colspan="3"></th>
        </tr>
      </thead> 

      <tbody>
        <% @schedules.each do |schedule| %>
        <%#  <tr>
            <td><%= schedule.title %></td>
        <%#    <td><%= schedule.start_time.strftime("%Y-%m-%d %H:%M") %></td>
        <%#    <td><%= link_to 'Show', schedule %></td>
        <%#    <td><%= link_to 'Edit', edit_schedule_path(schedule.id) %></td>
        <%#    <td><%= link_to 'Destroy',schedule_path(schedule.id), method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <%#  </tr>
        <% end %>
    <%#  </tbody>
    </table> %>

    <br>

    <%= link_to '日程入力', new_schedule_path %>

    <%= month_calendar events: @schedules do |date, schedules| %>
      <%= date.day %>

      <% schedules.each do |schedule| %>
        <div>
          <%= image_tag schedule.image_url, size: "80x60" if schedule.image? %>
          <p><%= link_to schedule.title, schedule %>
        </div>
      <% end %>
    <% end %>

</div>